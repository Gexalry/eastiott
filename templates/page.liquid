<article class="wrap" style="max-width:780px;padding:72px 28px;">
  <header style="text-align:center;margin-bottom:40px;">
    <h1 style="font-family:var(--serif);font-size:48px;font-weight:500;">{{ page.title }}</h1>
  </header>
  <div class="rte" style="font-size:16px;line-height:1.7;color:var(--ink);">{{ page.content }}</div>
</article>
